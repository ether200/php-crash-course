<?php

// INSERT

# INSERT INTO {table} (username, password) VALUES (usernameValue, passwordValue), () => You can add more than one value aka row

// UPDATE

# UPDATE {table}
# SET username = '', password = 'newpassword'
# WHERE id = 2

// DELETE

# DELETE FROM users 
# WHERE id = 2 => ~ optional ~ OR username = 'value'

// SELECT

# SELECT * FROM users WHERE id = 1

# SELECT * FROM users
# ORDER BY id DESC -> ASC DEFAULT

?>